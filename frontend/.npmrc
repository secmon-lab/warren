# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false

# Security: Enforce minimum package age (7 days = 604800000 milliseconds)
minimum-release-age=604800000

# Security: Disable all lifecycle scripts to prevent arbitrary code execution
ignore-scripts=true
